{
  "name": "bunker-pack-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Bunker pack generator.",
  "scripts": {
    "build": "node --check server.js && node --check script.js && node --check data.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.3",
    "socket.io": "^4.8.3"
  }
}
